/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

var setup = require('./setup'),
    pkg = require('../package.json');

var lib = setup.bind(this);
lib.setup = setup;
lib.version = pkg.version;

module.exports = lib;
