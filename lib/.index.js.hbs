/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

const setup = require('./setup'),
    pkg = require('../package.json');

let lib = setup.bind(this);
lib.setup = setup;
lib.version = pkg.version;

module.exports = lib;
